name: Keep Awake
on:
  schedule:
    - cron: '0 */6 * * *'
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: curl -s https://plateau-space-accessibility-viewer.streamlit.app/ > /dev/null
